# Журнал решений

Формат: дата — решение — причина. Новое решение добавляется сверху.

## 2026-09-16 — CI на GitHub Actions как в hours-bridge

Решение: `.github/workflows/ci.yml` (push в любые ветки + PR, job `verify`): unit-тесты,
`assembleRelease -PtargetAbis=arm64-v8a,armeabi-v7a`, APK в артефакты на 14 дней
(debug в CI нет: у владельца нет эмулятора, unit-тесты и так компилируют debug-код).
Ключ MapKit — через Secrets `MAPKIT_API_KEY`; без секрета сборка тоже зеленая
(карта-заглушка). `gradlew` помечен executable, иначе Linux-раннер падает с Permission denied.
Старое «CI нет, сборка вручную» (2026-09-14) считать устаревшим.
Причина: образец hours-bridge; каждый пуш проверяется автоматом.
Upd 2026-09-16: убран `android-actions/setup-android@v3` — первый прогон упал на нем
(не смог скачать cmdline-tools, в апстриме открыты те же жалобы); ubuntu-раннеры уже
содержат SDK с принятыми лицензиями, недостающее AGP докачивает сам.

## 2026-09-16 — Release-APK только arm ABI, фильтр на этапе упаковки

Решение: `build-apk.bat` (без опций, только телефонная сборка) передает
`-PtargetAbis=arm64-v8a,armeabi-v7a`, `app/build.gradle.kts` кладет в release-APK только arm
`.so` (141.7 → ~60 МБ). Debug/эмуляторных опций в батнике нет: у владельца нет эмулятора;
если debug/emulator-сборка понадобится для разработки — прямыми вызовами gradlew.
Из проекта ничего не вырезается — MapKit Full остается целиком, фильтр только упаковочный.
Причина: 94% APK — `libmaps-mobile.so` в 4 копиях под каждую архитектуру; x86/x86_64
на живых телефонах мертвый груз (~78 МБ).

## 2026-09-15 — Ребут: быстрый старт в ресивере + expedited-BootWorker

Решение: BootReceiver делает синхронный best-effort старт сервиса в окне
temp-allowlist BOOT_COMPLETED (~20 с) + ставит expedited-BootWorker
(setForeground первым делом); SystemForegroundService WorkManager объявлен
в манифесте с foregroundServiceType="location". Тихий рестарт после ребута
на API 34+ негарантирован (старт location-FGS из фона запрещен вне окна) —
страховка: MapScreen авто-стартует трекинг при открытии приложения.
Причина: три цикла проверки на эмуляторе API 36 (прямой старт из фона —
запрет; воркер без setForeground первым — STOP canceled; воркер без типа
в манифесте — краш SystemForegroundService).

## 2026-09-15 — Без ключа MapKit — заглушка, трекинг независим от карты

Решение: FogMapApp.isMapKitReady (ключ задан + initialize ok); без ключа
MapView не создается нигде (MapView без ключа роняет процесс AssertionError),
экраны показывают заглушку; авто-старт трекинга вынесен из-за ворот готовности
карты (трекинг локален, обязан работать без ключа).
Причина: два падения процесса, найденные проверкой на эмуляторе API 36.

## 2026-09-15 — Жесткие гейты старта FGS-трекинга

Решение: TrackingService.start/canTrack (FINE или COARSE), проверки в onCreate
и onStartCommand (без разрешений — stopSelf + START_NOT_STICKY), try/catch
вокруг startForeground; BootReceiver ловит все исключения.
Причина: старт FGS type=location без разрешений роняет процесс
SecurityException (найдено проверкой на эмуляторе API 36).

## 2026-09-14 — ЧП-1: импорт после MVP (изменение)

Решение: импорт Timeline/GPX вынести из MVP на этап после MVP.
Причина: решение владельца в сессии планирования; research/research-market
ранее требовали импорт в MVP — считать устаревшим, обновить при согласовании ЧП-1/ЧП-6.

## 2026-09-14 — ЧП-1: экраны MVP — полный набор

Решение: карта (главная) + статистика + история + детали трека + настройки
+ онбординг разрешений.
Причина: решение владельца, чтобы не переделывать навигацию.

## 2026-09-14 — ЧП-1: Kotlin + Compose

Решение: UI на Jetpack Compose.
Причина: решение владельца, современный стек для нового приложения.

## 2026-09-14 — OpenSpec 1.13.0 + команды для 3 IDE

Решение: глобальный CLI обновлен 1.12.0 → 1.13.0 (запись про 1.13.0 ранее
была преждевременной — фактически стояла 1.12.0); сгенерированы команды
для Cursor (.cursor/commands + skills), Devin (.devin/workflows + skills)
и Windsurf (.windsurf/skills, продублированы из Devin по образцу VoiceMind,
т.к. CLI 1.13.0 объединил Windsurf в Devin). Уточнение позже в тот же день:
Windsurf не нужен — удален, вместо него добавлен OpenCode (.opencode/commands
+ skills, там и ведется работа). Итоговый набор IDE: Cursor + Devin + OpenCode. Команда propose везде есть:
`/opsx-propose` (Cursor), `opsx-propose` workflow (Devin), skill (Windsurf).
`openspec/config.yaml` не тронут. Замечен баг CLI 1.13.0: список в `--tools`
через запятую не парсится — ставить по одному.
Причина: ревью по запросу владельца, унификация с другими проектами.

## 2026-09-14 — ЧП-3/ЧП-8: Full 4.42.0, схема БД, target 36, план согласован

Решение: MapKit Full 4.42.0 (выбор владельца с запасом); схема БД принята
(visited_cells/tracks/track_points + счетчики, Room); targetSdk + compileSdk 36,
minSdk 26. Юридическое: логотип/копирайты/кнопку «Открыть в Картах» не перекрываем
(туман только полигонами карты + ссылка в «О программе»). План по всем ЧП согласован,
следующий шаг — OpenSpec changes, затем код.
Причина: проверка доков Яндекса и Play + решения владельца.

## 2026-09-14 — ЧП-4/ЧП-6: без Play Services заглушка, без адаптации, детали импорта позже

Решение: без Play Services — заглушка; интервалы без адаптации;
UX импорта и детали экспорта — на этапе после MVP; в MVP холодного старта
через импорт нет. Шаблон ЧП из 8 разделов принят, отдельный ЧП под архитектуру
не нужен, критерий кода — все ЧП + 4 проверки + OpenSpec changes.
Причина: решения владельца.

## 2026-09-14 — ЧП-4/ЧП-5/ЧП-8: тесты, пустые состояния, уведомление

Решение: тесты — unit на сетку/статистику + ручной чек-лист;
первый запуск — карта в тумане с подсказкой; уведомление стандартное
с паузой, точный текст перед кодом.
Причина: решения владельца.

## 2026-09-14 — ЧП-2/ЧП-8: точки бессрочно, один модуль, без CI

Решение: сырые точки храним бессрочно; проект — один app-модуль с пакетами
по фичам, flavors dev/prod, ключ MapKit вне git; CI нет, сборка вручную
(устарело 2026-09-16: CI добавлен, см. запись выше).
Причина: решения владельца, простота соло-проекта.

## 2026-09-14 — ЧП-7: персональный проект, без Play Store

Решение: в Store не выкладываем — проект для себя и друзей, раздача через APK.
Декларация background location, видео-демо, store-нейминг не нужны.
Причина: решение владельца; снимает блок Play-модерации и нейминга.

## 2026-09-14 — ЧП-6: форматы GPX + Timeline после MVP

Решение: импорт/экспорт — GPX 1.1 + Timeline JSON, этап после MVP.
Причина: решение владельца.

## 2026-09-14 — ЧП-3: стандарт тумана, агрегация, viewport сразу

Решение: стиль — полупрозрачный темный слой с тонкой границей (без анимации);
на мелких зумах агрегация; viewport-culling + склейка + LRU-кэш делаем сразу.
Способ отрисовки предварительно — полигоны видимой области
(финал после проверки Lite/Full).
Причина: решения владельца + вывод tech-risks/FogBound.

## 2026-09-14 — ЧП-4/ЧП-5/ЧП-7: батчинг, счетчики, без шифрования

Решение: точки пишем пачками; статистика — материализованные счетчики;
удаление — кнопка сброса + удаление всего, шифрования БД и обработки root нет.
Причина: решения владельца, простота MVP.

## 2026-09-14 — ЧП-4: пауза, онбординг батареи, без эко-режима

Решение: при авто-трекинге есть пауза «не писать» (переключатель +
действие в уведомлении); онбординг про автозапуск/батарею делаем;
отдельного эко-режима в MVP нет — один режим 5–10 сек / 10–20 м.
Причина: решения владельца.

## 2026-09-14 — ЧП-5: история полная, офлайн не нужен, авторестарт

Решение: история — список + детали на карте, удаление, переименование;
офлайн-карт в MVP нет (только онлайн); после перезагрузки трекинг
перезапускается сам.
Причина: решения владельца.

## 2026-09-14 — ЧП-1/ЧП-6: импорт и экспорт после MVP

Решение: импорт Timeline/GPX и экспорт GPX — оба после MVP.
Причина: решение владельца; меняет вывод research про импорт в MVP.

## 2026-09-14 — ЧП-1: minSdk API 26

Решение: минимум Android 8 (API 26), target — по требованиям Play 2026.
Причина: решение владельца, баланс охвата и простоты.

## 2026-09-14 — ЧП-7: нагрузка до 50 пользователей, MapKit бесплатно

Решение: ориентир — до 50 пользователей всего (с запасом).
Коммерческая лицензия MapKit не нужна, хватает бесплатной (1000 DAU/сутки).
Причина: данные владельца + проверка тарифов Яндекса.

## 2026-09-14 — ЧП-1: только русский, portrait, без темной карты

Решение: MVP — русский язык, только portrait, темной темы карты нет.
Причина: решение владельца, минимум на старт.

## 2026-09-14 — ЧП-7: без аналитики, только крашлитика

Решение: поведение/аналитики нет, только отчеты о падениях; бэкенда нет.
Причина: решение владельца + приватность из vision.

## 2026-09-14 — ЧП-4: интервалы и фильтры стандартные

Решение: запись 5–10 сек / 10–20 м; в отброс accuracy > 25 м
и скорость > 150 км/ч, mock помечаем/игнорируем.
Причина: решения владельца, как предложено в tech-risks.

## 2026-09-14 — ЧП-5: статистика полным набором

Решение: площадь, дистанция, треки, время; разрезы день/неделя/все время.
Причина: решение владельца.

## 2026-09-14 — ЧП-2: радиусы 100/200/500 и площадь простым способом

Решение: радиусы открытия — пешком ~100 м, до 50 км/ч ~200 м, выше ~500 м;
площадь = кол-во ячеек × 0,01 км².
Причина: решения владельца, просто и достаточно для MVP.

## 2026-09-14 — ЧП-4: FusedLocationProvider

Решение: гео через FusedLocationProvider. Хвост: поведение без Play Services.
Причина: решение владельца, точнее и экономнее батарею.

## 2026-09-14 — ЧП-2: сетка 100 м + радиус по скорости

Решение: сетка фиксированная 100 м; вместо переменного размера ячеек —
адаптивный радиус открытия по скорости (пешком уже, на скорости шире).
Причина: предложение владельца про авто-размер дает эффект детализации,
но переменная сетка ломает ID/площадь/рендер; схема с радиусом проще,
владелец согласился.

## 2026-09-14 — ЧП-1: DI вручную

Решение: без Hilt/Koin, зависимости собираем вручную.
Причина: решение владельца, проще для соло-проекта.

## 2026-09-14 — ЧП-2: сетка WebMercator

Решение: ячейки — квадраты WebMercator.
Причина: решение владельца, просто считать и рисовать.

## 2026-09-14 — ЧП-1: Room + Coroutines/Flow

Решение: треки и ячейки храним через Room, асинхронность — Coroutines/Flow.
Причина: решение владельца, стандарт для локального хранения.

## 2026-09-14 — ЧП-1: трекинг всегда авто

Решение: запись идет всегда, когда есть разрешения, без кнопки старт/стоп.
Открытый хвост: авторестарт после перезагрузки, нужна ли пауза «не писать».
Причина: решение владельца.

## 2026-09-14 — ЧП-1/ЧП-2: ячейки навсегда + полный сброс

Решение: открытые ячейки навсегда, удаление трека их не закрывает.
Отдельно — кнопка «обнулить весь прогресс» с подтверждением.
Причина: решение владельца.

Решение: UI на Jetpack Compose.
Причина: решение владельца, современный стек для нового приложения.

## 2026-09-14 — Планирование по чекпоинтам, код после плана

Решение: ближайшие сессии — только детальный план по чекпоинтам
(`docs/plan.md`), код не пишем до согласования всех чекпоинтов.
Каждая сессия фиксируется в `docs/sessions.md`.
Причина: запрос владельца — не переделывать, все договоренности в доках.

## 2026-09-14 — Только трекер, без игры

Решение: никаких квестов, артефактов, сюжета, гильдий, лидербордов.
Причина: запрос владельца — «именно трекер с туманом».

## 2026-09-14 — Карта: Яндекс MapKit, только Android

Решение: Android + Яндекс MapKit, iOS вне скоупа.
Причина: качество карт/геокодера в РФ, русскоязычная аудитория.

## 2026-09-14 — Офлайн-first, все локально

Решение: туман и треки живут на телефоне (SQLite), без обязательного бэкенда.
Причина: приватность истории перемещений, работа без сети, простота MVP.

## 2026-09-14 — Туман: стартовать с GRID

Решение: сетка ячеек 100–200 м, рендер только видимой области.
Причина: масштабируемость и простота; плавная маска — позже как эксперимент.

## 2026-09-14 — Сначала доки, потом код

Решение: накапливаем vision/research/risks, код не пишем до созревания решений.
Причина: не переделывать.

## 2026-09-14 — OpenSpec обновлен 1.11.0 → 1.13.0

Решение: глобальный CLI обновлен до последней (latest на 2026-09-14 — 1.13.0).
Причина: инициализация нового проекта на актуальных шаблонах.
