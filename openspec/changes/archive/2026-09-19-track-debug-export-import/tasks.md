## 1. Сырой лог

- [x] 1.1 Добавить таблицу `raw_fixes` с DAO и миграцией и проверить что сборка и существующие тесты проходят
- [x] 1.2 Писать каждый fix в raw в `TrackingService.onRawLocation` синхронно с вердиктом и проверить что STAND и accuracy-фикс попадают в raw но не в `track_points`
- [x] 1.3 Покрыть запись raw unit-тестами на STAND, accuracy-отброс и jump и проверить что тесты зеленые

## 2. Экспорт батчем

- [x] 2.1 Собрать ZIP `meta.json + days/*.jsonl + fog + counters` за диапазон от-до и проверить что недельный экспорт содержит 7 файлов дней включая пустые
- [x] 2.2 Отдать ZIP через системный chooser из истории и проверить что файл уходит в Telegram и открывается на компе

## 3. Импорт с rebuild

- [x] 3.1 Перезаписать дни диапазона день-в-день без дублей day-atom и проверить что за дату остается ровно одна строка
- [x] 3.2 Сделать full rebuild тумана текущим кодом с пересчетом счетчиков с нуля в одной транзакции и проверить что лишнее пятно исчезает а статистика не двоится
- [x] 3.3 Принимать старый format version без отказа с перепрожкой текущим кодом и проверить что импорт старого файла успешен

## 4. Хранилище и входы

- [x] 4.1 Показать список raw по дням с размером и удалением дня/всего с объяснением про туман и проверить что чистка не меняет туман и треки
- [x] 4.2 Добавить в историю выбор диапазона для экспорта и системный picker для импорта и проверить сценарий выгрузил-залил-перезаписалось глазами на карте
