## 1. Лесенки в FogMask

- [x] 1.1 Добавить ступень z19 первой в near-цепочку (точное -> z19 -> z18 -> z16) и режимы `PRESENCE_Z19`/`PRESENCE_Z14` — проверка: unit-тест перебора точных возвращает пятна z19, перебора z19 — пятна z18
- [x] 1.2 Построить дальнюю лесенку ниже 11 (сначала z16, при переборе — z14, затем вуаль) — проверка: unit-тест тонкого маршрута на зуме 10 возвращает пятна z16, перебора z16 — пятна z14
- [x] 1.3 Обновить существующие ожидания режимов и прогнать suite — проверка: `./gradlew :app:testDebugUnitTest` проходит без падений

## 2. Подушки в оверлее

- [x] 2.1 Поднять `COARSE_CORNER_FRAC` до 0.5 и `COARSE_FEATHER_MULT` до 2.0, обновить `FogOverlayTest` — проверка: unit-тесты хелперов проходят; визуально на устройстве острых углов нет, точный коридор не размыт

## 3. Уровень пятен в диагностике

- [x] 3.1 Прокинуть уровень из `HolesResult` через `MapScreen` в `DevRenderStats` и писать `presence_z` в `RENDER agg` (0 — точное) — проверка: unit-тест агрегата содержит `presence_z`, existing `DevLogTest` проходят

## 4. Полевая проверка

- [x] 4.1 Пройти зум 10–11 над тонким маршрутом и пан 13–14 на свежей сборке с очищенными логами, снять `RENDER agg` — проверка: внизу змейка z16 вместо кляксы, ступени мельче, `presence_z` заполнено, `over_budget_hits = 0`, `avg_merge_ms` в прежних пределах
