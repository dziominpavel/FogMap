## 1. Подтверждение двойного счёта и сериализация flush

- [x] 1.1 Подтвердить гипотезу двойного счёта сверкой живых данных: пересчитать дистанцию/время дня из `track_points` строгим правилом (`trust >= 30` с обеих сторон) и сравнить со счётчиками дня; расхождение в разы при тех же точках подтверждает пересекающиеся батчи. Проверка: короткий отчёт с числами в change (эталон: 21.09 — 19.59 км против 11.24 км).
- [x] 1.2 Обернуть тело `flush()` в `TrackingService` в `Mutex`; best-effort пути (`onTaskRemoved`/`onDestroy` через `flushBlocking`) при занятом мьютексе пропускают запись без дедлока. Проверка: проект собирается, существующие тесты трекинга зелёные, повторная сверка 1.1 на новых живых данных сходится.
- [x] 1.3 Из отчёта 1.1: в `FogRepository` дистанция батча считается от последней записанной точки дня, якорь пробуждения идёт только в коридор тумана (`planBatch`); тест контракта. Проверка: `TrackStatsTest` — хвост дистанции не якорь; полевая сверка в 4.2.

## 2. Чанки rebuild по времени

- [x] 2.1 В `TrackDebugImport.importAndRebuild` резать чанки не только по 20 штук, но и по спану времени (~120 с): разрыв режет чанк. Проверка: `TrackDebugImportTest` — чанк через 3-часовую дыру делится, `time_s` тишину не впитывает.
- [x] 2.2 Проверить, что поведение ворот C не изменилось (хвост 32 покрывает чанк + лаг). Проверка: существующие `PendingGateTest`/`TrackDebugImportTest` зелёные.

## 3. Эко-метрики из снимка при импорте

- [x] 3.1 `parseZip` разбирает `counters.json` tolerant-парсером (битые строки пропускаются, неизвестные ключи терпятся). Проверка: `TrackDebugImportTest` — новый и старый формат читаются, битый JSON не роняет импорт.
- [x] 3.2 `importAndRebuild` восстанавливает эко-ключи (`ECO_METRICS`) за импортированные дни из снимка через `recordEco` с явной датой; сузить докстринг «снимок не используется» до геометрии. Проверка: unit-тест — после импорта ключи дня совпадают со снимком, геометрия пересчитана из raw.
- [x] 3.3 Отсутствие данных вместо нулей: медиана при пустых бакетах и карточка дня показывают «нет данных». Проверка: ручная проверка экрана на импорте старой выгрузки.
- [ ] 3.4 Повторный импорт дня 21.09 новым кодом: эко-карточка совпадает с живой, `time_s` вменяемый, дистанция сходится с пересчётом. Проверка: разбор нового экспорта, числа в change.

## 4. Приёмка

- [x] 4.1 Все юнит-тесты зелёные (`:app:testDebugUnitTest`), `assembleDebug` успешен. Проверка: вывод Gradle.
- [ ] 4.2 Обновить полевой отчёт: сверить прогноз rebuild с фактом после новых правок. Проверка: короткий отчёт с числами в change.
