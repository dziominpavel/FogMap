## 1. Диагностика и воспроизведение

- [x] 1.1 Воспроизвести перебор бюджета unit-тестом-спайком (сгенерировать > 800 точных дырок из синтетических клеток z21 во вьюпорте) и подтвердить, что текущий `holesForZoom` на зуме 13 возвращает > 800 дырок, а `MapScreen` при этом рисует 0 — проверка: временный тест падает/показывает скалу 800 -> 0
- [ ] 1.2 Снять полевой лог `RENDER slow_frame / agg` при зуме 13–13,6 над плотным районом и подтвердить `over_budget=true` — проверка: в DevLog есть W-событие с `over_budget=true` в окне пропадания тумана

## 2. Реализация fallback

- [x] 2.1 Перенести fallback в точную ветку `holesForZoom`: при переборе точных дырок вернуть `presenceHoles` по уже посчитанному срезу viewport на `MID_PRESENCE_Z`, переиспользуя срез без повторного `viewportCells` — проверка: unit-тест 3.1 проходит, обычный путь без перебора не изменился
- [x] 2.2 Сохранить второй уровень защиты: если и пятна z16 превышают `MAX_HOLES`, вернуть их как есть и оставить ветку глухой вуали в `MapScreen` без изменений — проверка: синтетический перебор пятен по-прежнему дает `overBudget=true` и 0 дырок на экране

## 3. Тесты

- [x] 3.1 Добавить unit-тесты в `FogPyramidTest`: перебор точных дырок на зуме 13 возвращает только дырки `MID_PRESENCE_Z` и непуст; отсутствие перебора возвращает точные дырки; перебор пятен остается перебором — проверка: `./gradlew :app:testDebugUnitTest --tests "ru.fogmap.FogPyramidTest"` проходит
- [x] 3.2 Прогнать существующие unit-тесты маски и пирамиды без регрессий — проверка: `./gradlew :app:testDebugUnitTest --tests "ru.fogmap.FogPyramidTest" --tests "ru.fogmap.FogGridTest" --tests "ru.fogmap.FogMathTest"` проходит

## 4. Полевая проверка

- [ ] 4.1 Вручную пройти зум 12 -> 13 -> 13,6 -> 14 над плотным районом в темной теме и проверить отсутствие черного экрана, плюс снять `RENDER agg` (ожидается `over_budget_hits > 0` при `holes > 0`) — проверка: туман виден на всех зумах, в логе fallback виден как `over_budget=true` с ненулевым `holes`
