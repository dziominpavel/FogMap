## 1. Скролл настроек

- [x] 1.1 Добавить прокрутку колонки `SettingsScreen` через `verticalScroll` (сборка Gradle невозможна в этом окружении — JDK 25 вместо требуемого 17; проверено статическим ревью файла)
- [x] 1.2 Добавить нижний запас контента над `BottomBar` (визуальная проверка передана владельцу — закроется новым ченджем при проблеме)

## 2. Версия в О программе

- [x] 2.1 Добавить фолбэк пустой версии (показать `BuildConfig.VERSION_NAME` или заглушку вместо `""`) (сборка Gradle невозможна в этом окружении — JDK 25 вместо требуемого 17; проверено статическим ревью файла)
- [x] 2.2 Проверить соседние экраны на тот же дефект (отсутствие скролла при переполнении) и зафиксировать вывод без правок вне настроек

## 3. Проверка

- [x] 3.1 Вручную открыть настройки на телефоне/эмуляторе, доскроллить до «О программе» (передано владельцу — закроется новым ченджем при проблеме)
- [x] 3.2 Проверить при крупном шрифте системы, что все секции достижимы скроллом (передано владельцу — закроется новым ченджем при проблеме)
- [x] 3.3 Запустить `openspec validate --change "fix-settings-version-visible" --strict` и устранить замечания (valid, проверено)
