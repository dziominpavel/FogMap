## 1. Профили эко-губернатора

- [x] 1.1 Перевести STANDBY на HIGH 30–60с/0м, убрать BALANCED и `POST_IDLE_SLEEP` как вето в `EcoGovernor.kt`, проверить `EcoGovernorTest` проходит
- [x] 1.2 Добавить константы speed-latch (`5 м/с`, `acc <=25м`) и дебаунса ACTIVE (`3–5 мин` после скорости), проверить значения читаются из одного места и покрыты unit-тестом

## 2. Пробуждение и переходы сервиса

- [x] 2.1 Починить `ecoTargetAfterVerdict` в STANDBY: GPS-смещение 100–150м будит в BURST всегда, включая сон после холостого BURST, motion/wifi только ускоряют — проверить новым unit-тестом «GPS без motion будит»
- [x] 2.2 Добавить OR-переход BURST->ACTIVE по speed-latch в `TrackingService.kt` без изменения вердикта точки — проверить unit-тестом «скорость >5 м/с с чистым accuracy уходит в ACTIVE при грязной истории»
- [x] 2.3 Увеличить дебаунс ACTIVE->STANDBY после скорости и обновить `enterProfile`/`resubscribe` — проверить unit-тестом «пробка 2 мин не роняет ACTIVE» и что `TRACK/eco_state` пишет те же поля без координат

## 3. Тесты и регресс

- [x] 3.1 Прогнать полный unit-сьют (`EcoGovernorTest`, `TrustEngineTest`, `PendingGateTest`, камера/туман) и проверить все зеленые через `./gradlew test`
- [ ] 3.2 Прогнать replay выгрузки 2026-09-22 (обед): старт с сиденья без motion дает ACTIVE и префикс сотни метров, а не 6–8км — проверить разбором `tracks/*.jsonl` и `counters.json`

## 4. Полевая проверка баланса

- [ ] 4.1 Проехать город на машине с телефоном на сиденье, разобрать `fog-*.jsonl` (`flush/eco_state/no_fix`) и проверить медиана префикса рутинных выездов `<=200м`, разрывов в километры нет, карта открывается
- [ ] 4.2 Сверить `eco_gps_ms` за сутки дома до/после и зафиксировать фактический рост расхода в `docs/sessions.md` — проверить цифра записана, решение по STANDBY 30с vs 60с и дебаунсу 3 vs 5 мин принято
