## 1. Спайк и ресурсы

- [x] 1.1 Проверить на устройстве `updateReason` камеры и API `UserLocationLayer` (видимость, кастомный pin, скрытие стрелки и круга, запрет headingMode) и зафиксировать фактические имена методов и поведение причины жеста DevLog-замером.
- [x] 1.2 Добавить растровую иконку красной точки (PNG по плотностям, красное ядро 12dp при масштабе слоя 1.5 — около 18dp на экране, белая обводка, тень) и проверить сборку и видимость значка в темной и светлой теме оболочки.

## 2. Курсор под вуалью

- [x] 2.1 Создать `UserLocationLayer` в `MapScreen` с сильной ссылкой, кастомным pin и скрытыми стрелкой и кругом точности, никогда не включать headingMode, и проверить на устройстве что красная точка отображается под вуалью а карта не вращается.
- [x] 2.2 Реализовать жизнь курсора (не прятать на паузе, приглушение 50% при тишине дольше 2 минут, отсутствие без фикса, заглушки без изменений) и проверить на устройстве паузой, тишиной и выключенным гео.

## 3. Показная дырка

- [x] 3.1 Добавить чистую функцию живой дырки в `FogMask` (радиус пешей кисти, правила зумов 13+/11–13/ниже 11) и проверить unit-тестами размер, лестницу зумов и неучастие в подсчетах данных.
- [x] 3.2 Подключить живую дырку в `MapScreen` к списку дырок перед проекцией тем же пером и проверить на устройстве что новая зона светится сразу а подтверждение из БД приходит без хлопка и без записи лишнего в статистику.

## 4. Follow-режим

- [x] 4.1 Реализовать follow-машину (ведение по умолчанию, пауза на любой жест, таймер 10 секунд, возврат SMOOTH 0.8с на том же зуме, FAB возвращает сразу, первый fix на 15f) и проверить на устройстве паном, зумом, ожиданием 10 секунд и тапом FAB.
- [x] 4.2 Проверить регресс севера (tilt 0, azimuth 0, поворот двумя пальцами ничего не меняет, дырки едут с картой) и проверить отсутствием программных петель в DevLog камеры.

## 5. Приемка

- [x] 5.1 Выполнить полевую проверку (новый квартал пешком, ручной осмотр с возвратом, пауза, ночь, stale) и проверить чек-лист сценариев из `specs/map-render/spec.md` и `specs/app-shell/spec.md`.
