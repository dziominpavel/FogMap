## 1. Ядро DevLog

- [x] 1.1 Создать синглтон `DevLog` (session id, seq, ts_wall/ts_mono, кольцевой буфер N=2000, уровни D/I/W/E, дубли в `Log` под `FogMap:<TAG>`) и проверить unit-тестом: seq строго растет, формат строки парсится `split('|', 7)`
- [x] 1.2 Добавить фоновый file-writer (очередь + один поток, append в `cacheDir/logs/fog-YYYYMMDD.jsonl`, лимит файла 2 МБ с событием `log_rotated`) и проверить на устройстве: события из буфера появляются в файле без блокировки UI-потока
- [x] 1.3 Добавить `PrefsKeys.DIAG_ENABLED` (default true), чистку файлов старше 24 ч при старте `FogMapApp` на фоновом потоке и ручную очистку; проверить: старые файлы удалены, новые пишутся только при включенном тумблере

## 2. Камера и рендер (главный подозреваемый)

- [x] 2.1 Инструментировать `CameraListener` в `MapScreen` счетчиками (события/сек, последние zoom/azimuth/tilt, отдельный `tiltFixCount`) без строковых аллокаций и проверить через adb: pinch двумя пальцами дает `CAMERA` агрегат с частотой и tilt-статистикой
- [x] 2.2 Добавить замеры вокруг пересчета дырок (`nanoTime`: merge `holesForZoom`, цикл `worldToScreen`, итог; поля cells/holes/nullProj/overBudget) с агрегатором 2 сек + мгновенные W-события при кадре > 500 мс или `overBudget`; проверить: долгий кадр виден и в агрегате, и отдельным W-событием
- [x] 2.3 Убрать покадровый `Log.d` со `String.format` из горячего пути (заменить агрегатором) и проверить: при активном пане logcat дает ~1 строку/2 сек вместо шторма

## 3. PERF и UI-действия

- [x] 3.1 Добавить агрегат `PERF` раз в ~10 сек (heap MB, число клеток в памяти, `logQueueDepth`) + детект jank через `Choreographer` (кадр > 700 мс → W-событие) + ANR-сторож с heartbeat 5 сек и проверить: искусственный `Thread.sleep(1000)` на UI дает W-событие jank
- [x] 3.2 Логировать тегом `UI` нажатия (FAB «Где я», компас, BottomBar, переходы навигации) и жизненный цикл MapView (ON_START/ON_STOP) по факту без агрегации и проверить: нажатие кнопки появляется в хвосте диагностики следующим событием

## 4. Фоновый трек-лог

- [x] 4.1 Добавить редкие события `TRACK` (flush: batch size, txn мс, newCells; PendingGate confirm/veto counts; старт/стоп сервиса, watchdog-рестарты, no-fix) не чаще 1/flush, координаты только как `x,y,z21`, и проверить: за 5 минут ходьбы в логе есть flush-события без lat/lon
- [x] 4.2 Добавить grep-проверку отсутствия `lat`/`lon` в вызовах `DevLog` (unit-тест по исходникам или ревью-чеклист в CI) и проверить: тест падает при добавлении lat/lon в payload

## 5. Экран диагностики и настройки

- [x] 5.1 Создать временный route «Диагностика» (хвост 50 событий из `StateFlow`, кнопки «Поделиться» через `FileProvider` и «Очистить», тумблер в Настройках) и проверить на устройстве: хвост обновляется вживую, Share отправляет JSONL, очистка удаляет файлы и буфер
- [x] 5.2 Пометить весь временный код префиксом `Dev`/`Diag` и добавить задачу на удаление; проверить: `grep -r "DevLog\|Diag" --include=*.kt` показывает только помеченные файлы для будущего revert

## 6. Полевая проверка

- [x] 6.1 Провести сценарий « pinch двумя пальцами 30 сек + кнопки» на устройстве, снять `adb logcat -s FogMap:*` и файл JSONL, убедиться что таймлайн отвечает на вопрос «пересчет или память» (видна связка CAMERA-частота → RENDER-dt → PERF-heap/jank); артефакт проверки — сохраненный лог и вывод из 3 строк
