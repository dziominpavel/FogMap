## 1. Отключение tilt-жеста

- [x] 1.1 Отключить tilt-жест при создании карты (`isTiltGesturesEnabled = false`) и удалить ветку корректирующего возврата tilt с флагом `snappingTilt` в `CameraListener`; проверить сборкой, что tilt-жест отсутствует, а rotate/pinch/пан работают
- [ ] 1.2 Проверить на устройстве: вертикальный свайп двумя пальцами 15 сек не меняет tilt (в `CAMERA`-агрегате tilt всегда 0.0), шторма и зависания нет. ОТЛОЖЕНО решением владельца 2026-09-19: на устройстве нет треков, карта под туманом, жесты визуально не проверить — проверить позже при наличии открытых клеток

## 2. Усиление диагностики

- [x] 2.1 Добавить нативный heap (`Debug.getNativeHeapAllocatedSize()`, поле `native_heap_mb`) в 10-секундный `PERF`-агрегат и проверить на устройстве: в логе рядом с dalvik-heap виден нативный ряд
- [x] 2.2 Добавить детектор шторма (`CAMERA/storm` уровнем W при > 60 событий/сек) и счетчик исходящих программных `move()` в `CAMERA`-агрегат без строковых аллокаций в горячем пути; проверить unit-тестом порога и отсутствием регресса существующих `DevLog`-тестов
- [x] 2.3 Перевести все диагностические `String.format` на `Locale.US` и проверить: числа в payload с точкой при русской системной локали, `DevLogPrivacyTest` зеленые

## 3. Дебаг-режим батника

- [x] 3.1 Добавить в `build-apk.bat` режим `debug [install]` (`assembleDebug`, без `-PtargetAbis`, выход `dist/FogMap-debug-latest.apk` + архивная копия) без изменения релизного пути по умолчанию; проверить: `build-apk.bat debug` собирает APK пакета `ru.fogmap.dev`, релизная сборка работает как раньше
- [ ] 3.2 Проверить сквозную схему расследования на дебаг-сборке: pinch 30 сек, съем `adb logcat | Select-String FogMap:` и JSONL через `run-as ru.fogmap.dev`, в таймлайне видна связка CAMERA-частота → moves → RENDER → PERF (включая нативный heap). ОТЛОЖЕНО решением владельца 2026-09-19 вместе с 1.2 (см. выше); `run-as ru.fogmap.dev` уже подтвержден рабочий

## 4. Приемка

- [x] 4.1 Прогнать `openspec validate --change "no-tilt-plus-diag"` (и `--strict`, если доступен) и убедиться, что все артефакты валидны
- [x] 4.2 Зафиксировать итог проверки в `docs/sessions.md`: tilt-жест отключен, шторм не воспроизводится, диагностика усилена; напомнить, что дебаг-сборка только для расследования, а временный код `Dev`/`Diag` удаляется отдельной задачей перед архивацией `dev-logging`
