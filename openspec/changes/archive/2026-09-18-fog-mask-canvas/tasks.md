## 1. Спайк проекции и перфа

- [ ] 1.1 Проверить `MapWindow.worldToScreen` на устройстве (UI-поток, null за камерой, phys px vs dp) и зафиксировать вывод логом скролла карты
- [ ] 1.2 Замерить Canvas с 300–500 скругленными дырками + перо 12–16px на Huawei, зафиксировать fps без просадки пана
- [x] 1.3 Выбрать финальные перо и радиус скругления по живому виду и записать в design как принятые значения

## 2. Кэш тумана в памяти

- [x] 2.1 Реализовать прелоад всех `visited_cells` в память при старте карты и проверить совпадением count с `cellCount()`
- [x] 2.2 Подписать слой на Flow-инкременты новых клеток и проверить открытием тестового трека без сдвига камеры
- [x] 2.3 Реализовать срез viewport в памяти с запасом и проверить отсутствием запросов Room на сдвиг (лог/профайлер)

## 3. Маска Canvas

- [x] 3.1 Нарисовать глухую вуаль 100% (`FF0F1419`/`FFF6F8F7`) первым кадром поверх `MapView` и проверить отсутствие голой карты на холодном старте
- [x] 3.2 Прорезать точные скругленные дырки из `FogRects.merge` с пером и минималкой 2px и проверить отсутствие рамок и лесенок вблизи
- [x] 3.3 Реализовать пятна присутствия z14 при zoom < 13 с плавным переходом и проверить неуход тропы на обзоре города
- [x] 3.4 Реализовать fallback в глухую вуаль при переборе бюджета и проверить сплошным туманом без краша

## 4. Камера и UI карты

- [x] 4.1 Зафиксировать tilt=0 во всех `move()` и жестах и проверить плоским видом сверху
- [x] 4.2 Добавить чип зума `15.2` над FAB и проверить обновлением из `CameraListener`
- [x] 4.3 Добавить компас (виден при azimuth > 10°, тап возвращает север с анимацией) и проверить поворотом двумя пальцами

## 5. Удаление полигонов и регресс
- [x] 5.1 Удалить `FogLayer`, ветвление уровней для показа и связанный кэш и проверить сборкой без ссылок на полигоны тумана
- [x] 5.2 Прогнать unit-тесты (`./gradlew test`) и проверить зеленую сборку
- [ ] 5.3 Прогнать живую приемку: старт, пан, зум 12–16, прогулка с неподвижной картой, ночь/день — без просвечивания, мигания и пропадания

## 6. Версия и О программе

- [x] 6.1 Поднять `versionCode`/`versionName` (1/0.1.0-mvp → 2/0.2.0-mvp), чтобы новая сборка ставилась поверх и отличалась
- [x] 6.2 Показывать в «О программе» версию и время сборки (`BUILD_TIME`), убрать ссылку на условия Яндекс Карт

## 7. Баги приемки маски

- [x] 7.1 Лестница пятен (z16 на 11–13, z14 ниже 11) вместо одного порога z14 — убрать гигантский поп на границе 13
- [x] 7.2 Пересчет дырок по target/azimuth, не только по зуму — туман едет с картой при пане одним пальцем
- [x] 7.3 Стрелка-компас в стиле Яндекс Карт вместо иконки-самолетика
