## 1. Ранние сигналы BURST без GPS

- [ ] 1.1 Добавить слушатель Significant Motion / шагов как `requestBurst(motion)` только из STANDBY и проверить на телефоне: взятие телефона и шаги уходят в BURST до смещения GPS, в ACTIVE — только по MOVING
- [ ] 1.2 Добавить `NetworkCallback` разрыва WiFi как `requestBurst(wifi)` только из STANDBY и проверить на телефоне: выход из дома уходит в BURST раньше таймера 90 с, при выключенном WiFi остальные слои будят самостоятельно
- [ ] 1.3 Заявить разрешения фазы 2 в манифесте и онбординге (объяснение, отказ не блокер) и проверить: без разрешений трекинг работает как фаза 1 без краша

## 2. Якорь и честный префикс

- [ ] 2.1 Персистить якорь STANDBY (lat/lon/time) в DataStore, восстанавливать в `onCreate`, обновлять при входе в STANDBY и при таймауте BURST, проверить киллом в STANDBY: рестарт считает смещение от якоря, а не от нуля
- [ ] 2.2 Считать префикс от якоря до первой MOVING во `flush` и дотягивать коридор от якоря, проверить реплеем утра 21.09: метрика равна полному `wake_m`, а не укороченным 181 м
- [ ] 2.3 Плохое accuracy (>25) не открывать туман, но учитывать для `requestBurst(gps)` + `rawFix`, проверить реплеем точки `acc 48.3`: уход в BURST есть, открытия тумана нет

## 3. Первая точка после тишины

- [x] 3.1 Добавить в TrustEngine ветку первой движущейся точки после долгой тишины как кандидата ворот C со стартовым доверием (телепорт-гейт для дальних выбросов оставить), проверить юнит-тестами: утренний выезд 500–1000 м идет в ожидание без вечного `jump`, выброс 4 км остается SUSPECT
- [x] 3.2 Прогнать существующие `TrustEngineTest`, `PendingGateTest`, `EcoGovernorTest`, `RawTraceTest` командой `./gradlew :app:testDebugUnitTest` и убедиться что все зеленые

## 4. Наблюдаемость и анти-флаппинг

- [ ] 4.1 Добавить `source` (`gps/motion/wifi/timeout/restart`) в `eco_state` и счетчик холостых BURST, проверить чтением JSONL: каждый `STANDBY->BURST` несет источник без координат, старый лог без `source` парсится как `gps`
- [ ] 4.2 Добавить дебаунс сна после неподтвержденного BURST (якорь обновлен, джиттер парковки не флаппит) и проверить на телефоне в покое: нет циклов BURST/TIMEOUT каждые 2 мин с `verdict STAND`
- [ ] 4.3 Показать в диагностике дня медиану префикса от якоря, источник пробуждения и холостые BURST, проверить открытием карточки после тестового выхода

## 5. Полевой баланс подъезд — парковка

- [ ] 5.1 Прожить 3–5 рутинных выходов подъезд -> парковка с метками времени и проверить по JSONL+треку: пеший отрезок и старт присутствуют, медиана префикса в бюджете 200 м, дыры нет
- [ ] 5.2 Прожить ночь в покое и проверить: `eco_fix/час` и холостые BURST не хуже базы фазы 1, в БД 0 точек за ночь
- [ ] 5.3 Зафиксировать решение по порогу тишины и STANDBY-интервалу (60/90) по итогам замеров и обновить `docs/decisions.md` одной записью
