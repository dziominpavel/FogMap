## 1. Стилизация слоя

- [x] 1.1 Задать красный растр обеим плейсмаркам слоя (`pin` и `arrow`) с центральными якорями и скрытым кругом точности, разделить независимые отказы стилизации, проверить сборкой `assembleDebug`
- [x] 1.2 Применять стиль заново на событии смены типа иконки (`PIN`/`ARROW`) и ставить stale-прозрачность обеим иконкам, проверить ручным ревью колбэка `onObjectUpdated`

## 2. Диагностика

- [x] 2.1 Писать тип иконки (`PIN`/`ARROW`) в `DevLog` на обновлениях слоя и явный неуспех стилизации с учетом `isValid()`, проверить наличием событий в выгрузке `fog-*.jsonl`
- [x] 2.2 Прогнать unit-тесты модуля (`./gradlew :app:testDebugUnitTest`) и проверить отсутствие регрессий туманной серии (`Fog*Test`, `LiveHoleTest`)

## 3. Полевая проверка

- [ ] 3.1 Проверить покой: открыть карту стоя, убедиться что виден красный кружок без стрелки, приложить скрин карты
- [ ] 3.2 Проверить движение: проехать на скорости с переходом SDK в `ARROW`, убедиться что осталась красная точка без желтой стрелки и позиция движется в реальном времени, приложить скрин карты и фрагмент лога с типами иконок
- [x] 3.3 Синхронизировать дельту `map-render` в основной спек при архивации и проверить `openspec validate --strict`
