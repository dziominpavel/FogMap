## 1. Единый экранный источник позиции и гейт правдоподобия

- [x] 1.1 Вынести чистую функцию гейта (accuracy ≤ 100 м → правдоподобный фикс) и покрыть unit-тестом в `app/src/test` (граничные: 100.0 проходит, 100.1 нет, отсутствие accuracy не проходит); верификация — `gradlew testDebugUnitTest` зелёный
- [x] 1.2 Собрать в `MapScreen` единый источник `liveLatLon`: позиция объекта слоя MapKit приоритетна, при молчании слоя — fallback Fused-подписка (старт/стоп по lifecycle ON_START/ON_STOP, первое значение — `lastLocation` через гейт); верификация — на устройстве: слой молчит, Fused даёт фикс ≤ 100 м → `liveLatLon` заполняется (DevLog `screen_pos source=fused accepted`), после onStop подписки нет (DevLog-событий не появляется)
- [ ] 1.3 Пропустить стартовый прыжок `moveToMyLocation` и fallback FAB через тот же гейт (фиксы хуже 100 м камеру не двигают); верификация — ручной прогон: фикс > 100 м → камера стоит на дефолте (DevLog `screen_pos rejected acc=...`), фикс ≤ 100 м → прыжок на зум 15

## 2. Fallback-курсор под вуалью

- [ ] 2.1 Добавить Compose-точку fallback-курсора в `Box` между `AndroidView` и `FogMaskOverlay`: растр `ic_my_location`, `ImageProvider`/`IconStyle` масштаб 1.5, якорь центр — та же визуальная форма, что у слоя; рисуется только когда позиция пришла от fallback; верификация — на устройстве в помещении: слой молчит, точка видна в показной дырке под вуалью
- [x] 2.2 Погасить fallback-точку в момент, когда слой отдал собственный объект (`onObjectAdded`/`onObjectUpdated`) — двойной точки на экране не бывает; верификация — ручной прогон: слой оживает → fallback исчезает, остаётся одна точка (сценарий «Одна точка на экране»)
- [ ] 2.3 Применить к fallback-точке существующее протухание (тишина > `SILENCE_RESET_S` → 50% непрозрачности, тот же `liveLastMs`/тик, что у нативной точки); верификация — ручной прогон: > 2 мин без фиксов точка полупрозрачная, свежий фикс возвращает 100%

## 3. Чип состояния геолокации

- [x] 3.1 Реализовать детектор системного мастера геолокации (вкл/выкл провайдеров при выданном разрешении) и состояние «Гео выключено» с высшим приоритетом среди состояний чипа; верификация — выключить гео в системе при выданном разрешении → чип показывает «Гео выключено» без перезагрузки экрана
- [ ] 3.2 Реализовать конечный автомат остальных состояний («Ищу спутники…» → «Точка получена» → «Протухло» по порогу 120 с = `TrustEngine.SILENCE_RESET_S`) вычислением чистой функции + unit-тест переходов; верификация — `gradlew testDebugUnitTest` зелёный, ручной прогон: свежий старт без фикса — «Ищу спутники…», фикс — «Точка получена», тишина > 2 мин — «Протухло»
- [ ] 3.3 Встроить чип в экран карты (компановка с существующим статус-чипом «Запись идёт» и чипом зума без наложений, обе темы), при существующих заглушках (нет разрешения/ключа/Play Services) чип не показывается; верификация — визуальный прогон всех четырёх состояний + случая заглушки в dark и light теме (сценарии чипа из спеки `map-render`)

## 4. Камера и follow через единый источник

- [ ] 4.1 Проверить, что стартовый прыжок первого fix (зум 15), возврат follow через 10 с и FAB «Где я» потребляют только гейтированный `liveLatLon` (без обходных путей к сырым Fused-координатам); верификация — ручной прогон сценариев «Первый fix как сегодня», «Фантомный фикс не увозит камеру», «FAB без позиции не двигает камеру», «FAB возвращает сразу» из спеки `app-shell`
- [ ] 4.2 Убедиться, что позиция слоя (без гейта) по-прежнему ведёт follow как раньше и fallback гасится при её появлении (нет разъезда камеры и точек); верификация — ручной прогон: улица, движение — камера следует за точкой слоя, зум не меняется без жеста

## 5. Полевая приёмка и граница с данными

- [ ] 5.1 Прогнать сценарный чек-лист первого запуска на устройстве: сброс прогресса дома → глухая вуаль + чип «Ищу спутники…», выход к подъезду → «Точка получена» + курсор + показная дырка, закрытие гео → «Гео выключено», > 2 мин тишины → «Протухло»; верификация — все сценарии дельт `map-render` и `app-shell` отмечены пройденными, DevLog без `style_failed`/крашей
- [ ] 5.2 Подтвердить нулевую зону записи: `git diff` не затрагивает `tracking/*`, `data/FogRepository.kt`, `data/db/*`, экспорт/импорт; после прогулки статистика дня (дистанция, клетки, эко-метрики) считается как до change; верификация — diff-обзор + сравнение карточки дня до/после на том же маршруте
