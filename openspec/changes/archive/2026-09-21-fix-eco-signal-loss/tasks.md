## 1. Доверие: статика по времени

- [x] 1.1 Перевести `trustHistory` в `TrackingService` на временное окно: чистка по возрасту (константа `TRUST_HISTORY_AGE_MS`, ориентир 60 с) плюс предохранитель по количеству (256) вместо `HISTORY_MAX = 8`. Проверка: проект собирается, `HISTORY_MAX` не используется в продовом коде, существующие тесты трекинга зелёные.
- [x] 1.2 В `TrustEngine` заменить условие `STATIC_MIN_POINTS` на авторитетное правило (якорь — самая старая точка возрастом ≥ 40 с, дрейф ≤ 25 м → STAND, иначе обычная ветка) и короткий предохранитель для `prev.state == STAND` (история короче окна, дрейф от самой старой точки ≤ 25 м → STAND). Проверка: `TrustEngineTest` проходит, поведение на каденции ~8 с не изменилось.
- [x] 1.3 Добавить unit-тесты частотной независимости: 1 Гц ходьба ~1.5 м/с после пробуждения → MOVING; 1 Гц стоянка с джиттером → STAND; остановка после движения уходит в STAND не позднее ~60 с. Проверка: новые тесты проходят.
- [x] 1.4 Проверить, что медиана и потолки не зависят от размера истории: тест «плотный поток 1 Гц не поднимает потолок и не отравляет медиану». Проверка: тест проходит, `Verdict.cap` на плотном потоке сопоставим с каденцией 8 с.

## 2. Ворота C: протухание только для движения

- [x] 2.1 Добавить `state` в `PendingGate.Item` и применять `MAX_PENDING_AGE_S` только к точкам, которые не MOVING (возврат в якорь остаётся для всех). Проверка: `PendingGateTest` — stale MOVING → confirmed, stale STAND → vetoed, `veto_return` без изменений.
- [x] 2.2 Прокинуть `state` из `unopenedTail` в `FogRepository.confirmPending` (колонка уже есть, миграции нет). Проверка: сборка и тест ворот C на хвосте с большой давностью и state=MOVING.

## 3. no-fix: живая диагностика тишины

- [x] 3.1 Починить предикат `starved` в `TrackingService.flush` (не учитывать `ecoSnapshot`, которая не пуста из-за `ECO_GPS_MS`) и писать в `TRACK/no_fix` фактический `gap_ms` вместо порога. Проверка: unit-тест на предикат/пейлоад, ручная проверка ветки по коду.
- [ ] 3.2 Проверить на устройстве: после провала доставки 5+ минут в JSONL есть `TRACK/no_fix` с фактическим gap, а `rejected_no-fix` в counters больше нуля. Проверка: разбор `fog-YYYYMMDD.jsonl` и `counters.json`.

## 4. Метрика префикса и разрывов

- [x] 4.1 В `TrackingService.flush` разделить случаи: пробуждение от якоря → `prefix_m` + счётчики префикса, разрыв `prev -> first` после тишины → `gap_m` + отдельные счётчики разрывов; убрать попадание разрывов в бюджет префикса. Проверка: unit-тест на расчёт и ручная сверка payload.
- [x] 4.2 Добавить `prefix_m` и `prefix_src` (anchor/gap) в `EcoLogPayload.flushPayload` и обновить `EcoLogPayloadTest`. Проверка: тест проходит, поле появляется в строке `TRACK/flush`.
- [x] 4.3 В `FogRepository` добавить `ECO_GAP_CM/N` и бакеты `ECO_PREFIX_B100_N`, `ECO_PREFIX_B200_N`, `ECO_PREFIX_B500_N`, `ECO_PREFIX_BHI_N`, включить в `ECO_METRICS`. Проверка: счётчики пишутся при flush и видны в `counters.json` разбора дня.
- [x] 4.4 `TrackRepository.ecoPrefixMedianM(range)` считать медиану по бакетам; в `HistoryScreens` показать медиану префикса (порог 200 м по медианному бакету) и отдельную строку разрывов (число и сумма). Проверка: unit-тест расчёта медианы по бакетам и визуальная проверка карточки дня.
- [x] 4.5 Убрать прежний `ecoPrefixAvgM` (среднее) из использования. Проверка: поиск по проекту не находит вызовов, экран показывает медиану.

## 5. Приёмка на устройстве

- [ ] 5.1 Прогнать день с рутинными выходами подъезд → парковка и поездкой, собрать `fog-YYYYMMDD.jsonl` и track-debug zip. Проверка: файлы разбора дня получены.
- [ ] 5.2 Сверить по собранным данным: доля отброшенного движения ниже ~10%, BURST доходит до ACTIVE при движении (а не 20 из 25 холостых), `no-fix` виден на провалах, медиана префикса ≤ 200 м, разрывы учтены отдельной строкой. Проверка: короткий отчёт с числами в change.
- [ ] 5.3 Если после фикса крупные потери остались — зафиксировать их как отдельную задачу про морозку FGS со ссылкой на измерения `no_fix`/разрывов. Проверка: задача создана либо явно записано, что потерь в норме.
