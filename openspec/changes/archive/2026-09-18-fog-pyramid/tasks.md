## 1. Схема и база

- [x] 1.1 Заменить схему на `visited_cells(x,y,z)` с индексом `(z,x,y)`, база z21, миграция вайпом (прогресс тестовый), проверить: чистая установка открывает клетки z21, старая БД мигрирует сбросом без краша
- [x] 1.2 Перевести площадь на веса относительно базы (родитель = сумма детей) с материализованными счетчиками, проверить unit-тестом: 4 ребенка = родитель по площади

## 2. Запись: кисть + коридор + компакшн

- [x] 2.1 Реализовать кисть 15/60/100 по скорости и доверию (ворота из `gps-trust-filter`) и коридор вдоль отрезков растром по клеткам, проверить: пешая нитка 11-33 м непрерывна, трасса 110 м между точками без дырок и без пятен 1 км
- [x] 2.2 Реализовать локальный компакшн 2x2 в транзакции (bounded) + фоновый воркер для каскада, чанкинг вставок, проверить: район схлопывается в родителей с той же площадью, время транзакции замерено

## 3. Чтение и рендер

- [x] 3.1 Читать пирамиду по зуму (издалека крупные, вблизи покрытие + мелочь), бюджет полигонов с fallback на вуаль, проверить: город целиком не лагает, улица показывает мелочь
- [ ] 3.2 Прогнать GPX-импорт через тот же pipeline, проверить: импорт открывает коридор, а не мусор
  - Отложено (2026-09-17, решение владельца): GPX-кода в проекте нет — это отдельная фича, вынести в будущий чендж. Pipeline (`openBaseCells`) уже готов ее принять.

## 4. Полевая калибровка

- [ ] 4.1 Пеший тест центр (z21 vs дырки) + автотест проспект/трасса, зафиксировать решение z21 или откат на z20 в `docs/sessions.md`; при откате меняется только константа базы
