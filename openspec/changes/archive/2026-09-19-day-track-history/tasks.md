## 1. День-атом в хранении

- [x] 1.1 Добавить find-or-create дня по дате в `TrackRepository`/`TrackDao` (SELECT строки за дату перед INSERT) и перевести `TrackingService.onCreate` и полуночный ролловер в `flush()` на него; верификация: два последовательных рестарта в один день дают одну строку в `tracks`, второй день после полуночи — новую.
- [x] 1.2 Обеспечить создание пустой строки дня при старте сервиса и при ролловере (0 точек, нулевые метрики) и резку поездки в 00:00 без хвоста прошлого дня; верификация: день только со статикой имеет строку с `pointsCount=0`, поездка 23:50–00:20 дает два дня со своими кусками.
- [x] 1.3 Обновить именование и чтение списка (`allTracks` порядок/имя-дата без изменений, старые обломки не трогаем); верификация: список истории показывает максимум одну строку на новую дату, старые даты лежат как были.

## 2. Ворота статики и честные метрики

- [x] 2.1 Дропать STAND-вердикт в `TrackingService.onRawLocation` до буфера (без записи в БД, без инкремента `rejected`; движок доверия в памяти шагает как раньше — иначе холодный старт не выйдет из STAND, см. design D2); прыжки `jump` продолжают сохраняться как недоверенные; верификация: STAND-точки не попадают в `buffer`, юнит-сьют зеленый.
- [x] 2.2 Подтвердить сумму дистанции/времени только по непустым flush-батчам движения (`FogRepository.batchDistance` + `timeS`), без отдельного аккумулятора; верификация: юнит-тест батча «12 км поездка + статика» дает 12 км и время только поездки.
- [x] 2.3 Проверить сшивку нитки на полуночи (круги по концам, без дырки тумана) на синтетической паре батчей через границу; верификация: ячейки обоих концов открыты, дистанция не задваивается и не теряется.

## 3. Дельта тумана и счетчики

- [x] 3.1 Читать дельту дня из существующих `area_cells_day_<дата>` для карточки дня и статистики (без новой таблицы/миграции); верификация: карточка дня после вылазки в новые ячейки показывает «+X км2», день без новых ячеек — 0.
- [x] 3.2 Привести семантику `StatsRepository`/счетчиков: `tracks` считает все строки дней включая пустые, `distance`/`time` — только движение; верификация: пустой день дает `tracks=1, 0 км, 0 мин`, день с поездкой — честные значения.

## 4. История и статистика UI

- [x] 4.1 Перестроить карточку дня в `HistoryScreens`: линия маршрута + дельта нового тумана + честные км/мин/точки + диагностика отбросов с разбивкой + секции утро/день/вечер, раскраска по доверию без изменений; верификация: ручной проход по дню с поездкой, пустому дню и дню с отбросами показывает три разных корректных состояния.
- [x] 4.2 Показать пустые дни в списке (строка с 0 км / 0 мин / 0 точек + диагностика «дома» vs «GPS резал»); rename/delete дня сохранить, удаление SHALL NOT трогать туман; верификация: удаление дня убирает строку, `visited_cells` и площадь не меняются.
- [x] 4.3 Обновить `StatsScreen`/пустые состояния под новую семантику (треки = дни, время = движение); верификация: вкладки день/неделя/все время сходятся со списком истории.

## 5. Тесты и приемка

- [x] 5.1 Расширить юнит-тесты (`TrackStatsTest`, `HistoryUiTest`, тесты репозитория): find-or-create идемпотентность, дроп STAND, дистанция/время без статики, резка в полночь; верификация: `./gradlew :app:testDebugUnitTest` зеленые.
- [x] 5.2 Прогнать связанные существующие suites (`TrustEngineTest`, `PendingGateTest`, `LocationFilterTest`, `FogGridTest`, `DatabaseTest`) и зафиксировать отсутствие регрессий тумана/ворот; верификация: все suites зеленые, `openspec validate --change day-track-history` без ошибок.
