## 1. Ворота A+B (сигналы, без лагов туманa)

- [x] 1.1 Добавить телепорт-гейт (~500 м между соседними доставками → SUSPECT независимо от dt) и сброс доверия после тишины (dt > ~2 мин → стартовое доверие, первая точка не открывает), проверить unit-тестами серий: аэропорт через тишину, выезд из тоннеля, честная трасса 150 км/ч
- [x] 1.2 Хранить полный вердикт в точке (state + причина, миграция колонок), проверить unit-тестом схемы ключей и дефолтами старых точек

## 2. Ворота C (отложенное открытие)

- [x] 2.1 Реализовать очередь ожидания 2–3 точки во flush: туман открывается только подтвержденным, возврат в якорь дает вето, догоняющее открытие после убийства процесса, проверить unit-тестами: круговой вылет не открывает, ровная трасса открывает с лагом, счетчики консистентны
- [ ] 2.2 Прогнать записанный трек с аэропортом через новые ворота и зафиксировать «открылось/не открылось» в `docs/sessions.md`

## 3. Сутки-чанки и цветная история

- [x] 3.1 Резать новые треки календарными сутками (имя — дата; старые треки-процессы не трогать; коридор через полночь не тянуть), секции утро/день/вечер в деталях дня, проверить: поездка через полночь дает два чанка без сшивки
- [x] 3.2 Красить линию трека по доверию в деталях истории (доверенные — синим, недоверенные — серым пунктиром), на карту не тащить, проверить вручную на треке с аэропортом

## 4. Пауза только в Настройках

- [x] 4.1 Убрать FAB паузы с карты и действие из уведомления (статусное), добавить секцию «Запись» с тумблером в Настройки; флаг, watchdog и счетчик `paused` без изменений, проверить: случайных включений с карты/шторки больше нет, пауза из Настроек работает

## 5. Полевая приемка

- [ ] 5.1 День езды + ночь статики: телепортов в тумане нет, сутки-чанк читается, серый пунктир только на реальных выбросах; зафиксировать в `docs/sessions.md`, откалибровать пороги (500 м / 2 мин / лаг)
