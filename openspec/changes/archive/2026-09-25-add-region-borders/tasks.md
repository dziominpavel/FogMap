## 1. Метрики: площадь полигона и широтная поправка

- [x] 1.1 `region/Regions.kt`: вычислять площадь региона в рантайме (шалеус по не вложенным кольцам, лениво один раз), удалить 13 констант `totalAreaKm2`
- [x] 1.2 `fog/FogGrid.kt`: конвертация клеток → км² с поправкой `cos²(широты)`; параметр широты в `areaKm2`, константа средней широты Беларуси для глобальных метрик
- [x] 1.3 `data/RegionProgressRepository.kt`: знаменатель — площадь полигонов, широта региона — центроид колец
- [x] 1.4 Прогнать вызывающие sites: `TrackRepository.Stats`, `AchievementRepository`, история дня — глобальная средняя шириота; проверить, что `StatsScreen`/`HistoryScreens` не требуют правок API
- [x] 1.5 `achievement/Achievements.kt`: порог «100% региона» — `percent >= 99`; пороги 10/50% и 10/100/1000 км² не трогать

## 2. Настройка

- [x] 2.1 `data/SettingsRepository.kt`: ключ `region_borders` (default `false`) + Flow/setter по образцу существующих
- [x] 2.2 `ui/screens/SettingsScreen.kt`: новая секция «Карта» с переключателем «Границы регионов на карте»

## 3. Отрисовка границ

- [x] 3.1 Сборка колец для отрисовки: фильтр вложенных колец, явное замыкание незамкнутых, кэш на `Regions.ALL`
- [x] 3.2 Новый слой (например, `map/RegionBordersLayer.kt`): `PolylineMapObject` по типам — республика жирная сплошная, область штриховая, город тонкая сплошная; единая палитра + outline
- [x] 3.3 `ui/screens/MapScreen.kt`: подписка на Flow настройки, добавление/прятанье объектов через `isVisible`, живое обновление без перезапуска
- [x] 3.4 Зум-гейт: городские контуры скрыты при `camZoom < 10` из слушателя камеры (порог начальный, подлежит подстройке)
- [x] 3.5 Убедиться, что слой живёт только на главной карте (карта истории дня не трогается)

## 4. Тесты

- [x] 4.1 Unit: эталонная площадь полигона (сверка по паре регионов с известными числами)
- [x] 4.2 Unit: логика выбора колец (вложенные-дырки пропускаются, отрезки остаются, незамкнутое кольцо замыкается)
- [x] 4.3 Unit: при полном открытии региона процент ≈100 в допуске; `percent >= 99` открывает ачивку 100%

## 5. Верификация и подгонка

- [x] 5.1 `$env:JAVA_HOME = "C:\Users\Dziom\.gradle\jdks\eclipse_adoptium-17-amd64-windows.2"`; `.\gradlew.bat :app:compileDebugKotlin` и `.\gradlew.bat :app:testDebugUnitTest` — зелёные
- [x] 5.2 Сборка/установка `build-apk.bat install`, включить тумблер, снять скриншот под вуалью тумана (день и ночь)
- [ ] 5.3 По скриншоту подогнать цвета, толщины, dashLength и порог зума — решение владельца
- [ ] 5.4 Проверить новые числа процентов на экране прогресса (Гомель-область ~1,15% вместо прежних ~4%)
- [x] 5.5 Записать изменения в `CHANGELOG.md` → `## [Unreleased]` (без бампа версии)
