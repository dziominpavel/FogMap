## 1. Лесенка fallback в FogMask

- [x] 1.1 Расширить результат `holesForZoom` режимом (PRECISE / PRESENCE_Z18 / PRESENCE_Z16) и реализовать цепочку точное -> z18 -> z16 -> вуаль с обобщенной веткой грубых родителей — проверка: unit-тест перебора точных возвращает пятна z18, перебора z18 — пятна z16
- [x] 1.2 Обновить ожидания существующих тестов лестницы для зоны fallback (чистый средний зум 11–13 без перебора остается z16, `liveHoles` без изменений) — проверка: `./gradlew :app:testDebugUnitTest --tests "ru.fogmap.FogPyramidTest" --tests "ru.fogmap.LiveHoleTest"` проходит

## 2. Гистерезис режима в MapScreen

- [x] 2.1 Хранить режим показа в `remember` слоя: уход в пятна при точных `> MAX_HOLES`, возврат при точных `<= RETURN_THRESHOLD` (600), сброс режима при уходе зума из зоны >= 13 — проверка: unit-тест дребезга (колебание 790/810 не переключает режим каждый кадр, возврат только ниже порога)
- [x] 2.2 Прокинуть режим из результата `holesForZoom` в проекцию и отрисовку без смены поведения fail-closed и ключей пересчета — проверка: пан на одном зуме не дает просвечивания голой карты

## 3. Мягкие грубые пятна в оверлее

- [x] 3.1 Масштабировать скругление и перо от размера дырки в px (точные — текущие 6/14, грубые — увеличенные) — проверка: unit-тест хелперов `cornerPxFor`/`featherPxFor` проходит; визуально на устройстве в темной и светлой теме острых углов ступеней нет, точный коридор не размыт

## 4. Флаг fallback в диагностике

- [x] 4.1 Добавить счетчик `fallback_hits` в `DevRenderStats`/`RENDER agg` отдельно от `over_budget_hits`, без покадровых W-событий на fallback — проверка: в логе fallback виден как `fallback_hits > 0` при `over_budget = false`, existing тесты `DevLogTest` проходят

## 5. Полевая проверка

- [x] 5.1 Пройти пан на 13.4–13.8 над плотным районом на свежей сборке с очищенными логами и снять `RENDER agg` — проверка: хлопков точное/пятна от одного касания нет, `fallback_hits > 0` при `over_budget_hits = 0`, `avg_merge_ms`/`avg_proj_ms` в прежних пределах
