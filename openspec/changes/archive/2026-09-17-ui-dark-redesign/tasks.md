## 1. Тема и токены

- [x] 1.1 Добавить строковый `theme_mode` (`dark|light|system`) в DataStore с дефолтом `dark` и проверить сохранением и чтением после перезапуска
- [x] 1.2 Создать `ui/theme/` с токенами dark/light и `FogMapTheme`, проверить переключение без перезапуска на главном экране
- [x] 1.3 Перевести `themes.xml` на DayNight с фоном `#0F1419` и проверить отсутствие белой вспышки сплэша

## 2. Навигация и карта

- [x] 2.1 Добавить иконки Material Symbols в `BottomBar` и индикатор записи, проверить отображение 4 разделов с подписями
- [x] 2.2 Заменить кнопки карты на FAB-стек «Где я» и «Пауза/Продолжить» со статус-чипом сессии, проверить управление одной рукой и видимость паузы
- [x] 2.3 Оформить заглушки карты (нет Play Services / нет гео / нет ключа) карточками с CTA, проверить каждый сценарий заглушки

## 3. Контентные экраны

- [x] 3.1 Переложить статистику на сетку 2x2 и hero-карточку с пустым состоянием, проверить значения из счетчиков и CTA
- [x] 3.2 Переложить историю на карточки треков и единый стиль деталей с rename/delete, проверить удаление без изменения тумана
- [x] 3.3 Переложить настройки на секции Внешний вид / Данные / О программе с `SegmentedButton` темы и опасной зоной сброса, проверить выбор темы и сброс с подтверждением
- [x] 3.4 Добавить степпер 1-2-3 с прогрессом в онбординг, проверить прохождение FINE → BACKGROUND → батарея

## 4. Ночная карта

- [x] 4.1 Провести спайк `setNightModeEnabled` с цветами тумана из темы на зумах 14–16 и зафиксировать альфа вуали и цвет границы
- [x] 4.2 Синхронизировать ночной режим карты с темой с fallback на дневную карту при плохом контрасте, проверить переключение dark/light/system

## 5. Ассеты и приемка

- [x] 5.1 Нарезать launcher через Image Asset и WebP-иллюстрации в `drawable-nodpi`, переключить иконку в манифесте, проверить иконку 48dp и small-icon уведомления — ВЫПОЛНЕНО вручную (эквивалент Image Asset: `mipmap-*/ic_launcher*` + `mipmap-anydpi-v26`, `drawable-nodpi/*.webp`, `drawable-*dpi/ic_stat_fog.png`, smallIcon в обоих сервисах, проверено `aapt2 link`)
- [ ] 5.2 Прогнать приёмку стиля на устройстве (иконка 48dp, small-icon уведомления, отсутствие белых миганий) по чеклисту редизайна
