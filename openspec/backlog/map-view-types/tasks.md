## 1. Разведка (до решения о старте)

- [ ] 1.1 На устройстве выставить `mapType = SATELLITE` и `HYBRID`, снять
  скриншоты с открытой вуалью — проверить, видны ли дырки и границы регионов
- [ ] 1.2 Проверить night mode + спутник: что темнее/читаемее, нужен ли
  отдельный стиль вуали для фотофона
- [ ] 1.3 Оценить расход трафика спутника на типичный выезд и решить,
  нужно ли предупреждение/галочка «только по Wi-Fi»

## 2. Решения (перед кодом)

- [ ] 2.1 Где живёт переключатель: пункт в `SettingsScreen` (по образцу
  «Границы регионов») или кнопка на `MapScreen`
- [ ] 2.2 Применять ли вид карты к мини-карте в `HistoryScreens`
- [ ] 2.3 Фиксируем решения в `docs/decisions.md` и design change

## 3. Данные и настройка

- [ ] 3.1 `PrefsKeys.MAP_VIEW_TYPE` (string, default `map`) + геттер/сеттер
  в `SettingsRepository`
- [ ] 3.2 Передать значение в `MapScreen` и выставить `mapWindow.map.mapType`
  при создании `MapView`, при смене — без пересоздания (переживает follow/фаб)

## 4. UI

- [ ] 4.1 Segmented button «Схема / Спутник / Гибрид» в настройках (или
  кнопка на карте — по решению 2.1)
- [ ] 4.2 Обе темы, русский текст, состояние применяется сразу
- [ ] 4.3 (опц.) Подсказка про трафик при первом выборе спутника

## 5. Верификация

- [ ] 5.1 Ручной прогон: смена вида на лету — туман, курсор, границы
  регионов, чипы не ломаются; перезапуск приложения — вид сохранён
- [ ] 5.2 Проверка на зумах 12–17: дырки тумана читаемы на спутнике, в
  тёмной и светлой теме
- [ ] 5.3 `testDebugUnitTest` зелёный, `compileDebugKotlin` без новых
  варнингов
